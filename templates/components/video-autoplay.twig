<video
	preload="auto"
	poster="{{poster}}"
	autoplay
	playsinline
	muted
	loop
	class="{{class}}"
	width="{{video.width}}"
	height="{{video.height}}">

	<source src="{{video.url}}" type="{{video.mime_type}}" />
</video>
