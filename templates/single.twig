{% extends "index.twig" %}

{% block content %}
<article class="post-type-{{ post.post_type }}" id="post-{{ post.ID }}">
	{% include "general/page-title-post.twig" with {'title' : post.title} %}

	<div class="container mb-xxl">
		{{post.content}}
	</div>

	{% include "components/prev-next-posts.twig" %}

</article>
{% endblock %}
